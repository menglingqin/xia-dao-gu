name: Archived - S3 deploy (moved)

# 归档的 S3 部署工作流。原始完整工作流已保留在版本历史中。

on: [workflow_dispatch]

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: echo "This workflow has been archived and moved to deploy/archive/. If you need the original workflow, retrieve it from git history."
